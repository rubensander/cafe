Hallo!

Hier eine kurze Installationsanleitung:

Nachdem du das Verzeichnis zu deinem Account hinzugefügt hast, 
kannst du auf deinem Rechner einen neuen Ordner "cafe" erstellen und dort das Projekt via SSH/HTTP pullen.
Falls du deinen Rechner z.B. schon per SSH in deinem Account registriert hast, führe in der Git Bash die Befehle:

$ git init
$ git remote add -f cafe-remote git@github.com:AinaIanemahy/cafe.git
$ git pull cafe-remote

Dann musst du die Tools Node.js und npm herunterladen, um Node.js auszuführen und einen Server simulieren zu können:

https://nodejs.org/en/

Wenn alles geklappt hast kannst du im Ordner "cafe" dann die Powershell öffnen (im Explorer: Datei -> PowerShell öffnen)
und folgenden Befehl eingeben:

> npm install express ws

Dann kannst du den Server mit 

> node server.js 

starten. Nun sollte im Browser die Startseite aufrufbar sein, unter der Adresse "localhost".

Java kann kompiliert werden mit

> javac -d java-project\bin -cp java-project\lib\* java-project\src\cafe\*.java

und gestartet werden mit

> java -cp "java-project\bin;java-project\lib\*" cafe.Game